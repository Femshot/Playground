#include<stdio.h>

/**
 * main - Entry point
 * Description: Displays "123" & "Bye" to stand-out, both on separete lines
 * Return: 0
 */

int main(void)
{
	char L = 'B', M = 'y', N = 'e';

	putchar(49), putchar(50), putchar(51), putchar('\n'); 
	putchar(L), putchar(M), putchar(N), putchar('\n');

	return (0);
}
