#include "main.h"

int _isupper(int c)
{
	int i;
	int j;

		for (i = 'A'; i <= 'Z'; i++)
		{
			if (c == i)
				j == 1;
				break;
		}
		for (i = 'a'; i <= 'z'; i++)
		{
			if (c == i)
				j == 0;
				break;
		}

	return (0);
}
